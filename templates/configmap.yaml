apiVersion: v1
kind: ConfigMap
metadata:
  name: planpal-config
  labels:
    {{- include "planpal.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
