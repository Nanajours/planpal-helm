PlanPal {{ .Chart.AppVersion }} installed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

{{ if .Values.ingress.enabled -}}
URL:  https://{{ .Values.ingress.host }}

The ALB takes 2-4 minutes to provision. Watch for an ADDRESS:
  kubectl get ingress -n {{ .Release.Namespace }} -w
{{- end }}

Check everything came up:
  kubectl get pods -n {{ .Release.Namespace }}
  kubectl get externalsecret -n {{ .Release.Namespace }}

The ExternalSecrets must reach SecretSynced before the app can start; the
database URL and every credential arrive that way.

Health, end to end:
  helm test {{ .Release.Name }} -n {{ .Release.Namespace }}
